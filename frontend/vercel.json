{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
{
  "installCommand": "npm install --include=dev",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}